// Sustituido por `widgets/metric_card.dart` (MetricCard) en el
// rediseño 2026. Se deja este archivo solo para no romper ningún
// import histórico; no se usa en ninguna pantalla actual.
export '../../../widgets/metric_card.dart';
